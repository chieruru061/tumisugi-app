## 機能名
- 日本語名: お積みもの追加
- 英語名 : Tumi Add

## 概要
お積みものを新規追加するための画面

## トリガー
- お積みもの一覧画面.追加ボタンクリック

## 機能一覧
※DDL: ドロップダウンリスト

### 初期表示
1. controllerは画面情報を返却する
    1. カテゴリDDLに表示する、カテゴリ一覧
    2. 積み状態DDLに表示する、ステータス一覧
    3. プラットフォームDDLに表示する、プラットフォーム一覧
2. Viewは受け取った情報をDDLに設定

### 検索
1. ユーザは検索条件を入力し、検索を行うことができる。
2. 検索条件
    1. カテゴリDDL
    2. 積み状態DDL
    3. プラットフォームDDL
    4. タイトルテキストボックス
3. 検索ボタン押下で、controllerにパラメータとして検索条件を渡す
4. controllerは検索条件に合致するお積みもの一覧情報を返却する
5. Viewは受け取ったお積みもの一覧情報を10件ずつ表示する

### 追加ボタン
1. 入力バリデーションを実施する
2. バリデーションエラーがない場合、controllerにフォーム情報を渡す
3. controllerはお積みもの情報をDBに登録する
4. 登録成功後、お積みもの一覧画面に遷移する

### 戻るボタン
1. お積みもの一覧画面に遷移する

### その他

## 表示項目（一覧）
- 画面名: お積みもの追加
- タイトルテキストボックス
- カテゴリDDL
- お積みもの状態DDL
- プラットフォームDDL
- メモテキストボックス
- 追加ボタン
- 戻るボタン

## バリデーション
- TODO: 将来追加予定
  - 長さチェック
  - 禁止文字チェック(例: SQLインジェクション対策,XSS対策)
  - 空白・全角スペースの正規化
  - 文字種の正規化(例: 全角→半角)

## API / コントローラ設計（例）
- Controller: GamesController
  - GET /Games/Index?page={}&pageSize={}&sort={}&genre={}&status={}
  - Response: RazorView

## 例外処理とログ
- 例外はサービス層でキャッチして、ユーザには一般的なメッセージを表示
- 詳細はサーバログに出力（例: ILogger）(TODO: 将来追加予定)

## 実装メモ / 備考